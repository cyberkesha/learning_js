const promt = "hello world";

function show(i_line) {
  console.log(i_line);
}

show(promt);

const x = "first line";
const y = "second line";

const concat_line = (x, y) => { return x + y };

console.log(concat_line(x, y));

// I added this comment later
// I added one more comment
